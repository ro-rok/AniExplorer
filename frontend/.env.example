# API Configuration
# Base URL for the backend API
VITE_API_BASE_URL=http://localhost:8000

# Feature Flags
# Enable/disable animations (set to false for reduced motion)
VITE_ENABLE_ANIMATIONS=true

# Use mock API instead of real backend (useful for development)
VITE_MOCK_API=false
